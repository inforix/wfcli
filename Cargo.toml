[workspace]
members = ["crates/wfcli"]
resolver = "2"

[workspace.package]
version = "0.2.0"
edition = "2021"
license = "ISC"

# The profile that 'dist' will build with
[profile.dist]
inherits = "release"
lto = "thin"
